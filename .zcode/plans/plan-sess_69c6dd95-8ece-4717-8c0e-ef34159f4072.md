## Plan: Minimal README update for the staged 1.9.2 changes

Keep edits surgical — only fix what's now inaccurate or omits a headline new feature. No prose rewrites, no new sections.

### 1. Testing section (lines 106–134)
- Total **704 → 859**.
- Add one row: `test_smtp.py` — 26 tests (SMTP session, message building, reply/forward, persistence, rate limiting).
- Update the changed counts only: `test_web.py` 177→215, `test_cache.py` 97→135, `test_email_reader.py` 72→83, `test_idle_monitor.py` 63→67, `test_auth.py` 42→45, `test_imap.py` 120→155.

### 2. Features section — email detail & actions bullets (lines 244–246)
- Append to the **Email detail** bullet: it now shows the **conversation** (threaded replies, "Sent" badge on outbound).
- Append to the **Email actions** bullet: **reply** and **forward** via a compose modal (recipient pre-filled on reply, quoted block on forward, threaded headers; sent message saved locally; rate-limited to 10 sends/min).

### 3. Configuration `.env` table (lines 404–411)
Add two rows for reply/forward SMTP (reusing IMAP credentials):
- `SMTP_SERVER` — `smtp.gmail.com`
- `SMTP_PORT` — `465`

That's it — no changes to Database, Performance, tagline, or TOC.